# ---------------------------------------------------------------------------
# NetDevOps Engine — Environment Template
# Copy this file to `.env` and populate with real credentials.
# NEVER commit the populated `.env` file to version control.
# ---------------------------------------------------------------------------

# Device login credentials (used for all inventory hosts unless overridden)
NET_USER=admin
NET_PASS=changeme
